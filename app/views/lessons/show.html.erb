<h1><%= @lesson.name %></h1>
<h4><%= @lesson.content %></h4>
<h4><%= @lesson.number %></h4>

<p><%= link_to "Next Lesson", week_lesson_path(@week, @lesson.next_lesson)%></p>

<p><%= link_to "Return to week", week_path(@week) %></p>
<p><%= link_to "Edit", edit_week_lesson_path(@lesson) %></p>
<p><%= link_to "Delete", week_lesson_path(@lesson), :data => {:confirm => "are you sure?", :method => "delete"} %></p>
